# ─────────────────────────────────────────────────────────────
# TripIn Web 环境变量模板
# 默认「clone 后直接能跑」：不需要任何 .env.local 也会连服务器后端、并从后端取到地图 Key。
# 只有想覆盖默认（连本地后端 / 用自己的高德 Key）时，才复制本文件为 .env.local 并填写。
# .env.local 不会被提交到 git。
# ─────────────────────────────────────────────────────────────

# 后端 API 地址：默认（不设此变量）即连服务器后端，无需改动。
#   只有想连「本地后端」（需自起本地 Postgres + jar）时才放开下面一行：
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1

# 高德地图 Web JS Key —— 工作台「真实地图」用。
# 留空即可：默认会向后端 /config/web 索取（Key 只存在服务器环境变量里，仓库不含任何 key）。
# 只有想用「自己的」Key 时才在这里填（会覆盖后端下发的）。对应移动端 EXPO_PUBLIC_AMAP_JS_KEY。
# NEXT_PUBLIC_AMAP_JS_KEY=
# NEXT_PUBLIC_AMAP_JS_SECURITY_CODE=

# 说明：NEXT_PUBLIC_* 是「构建时」注入浏览器端的——next dev 改完要重启；打包前先设好再 next build。
