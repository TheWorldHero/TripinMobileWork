# ─────────────────────────────────────────────────────────────
# TripIn Web 环境变量模板
# 复制本文件为 .env.local 后按需修改（.env.local 不会被提交到 git）。
# ─────────────────────────────────────────────────────────────

# 后端 API 地址：同一份代码，改这一行即可在「本地」/「服务器」之间切换。
#
#   本地后端：
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
#
#   服务器后端（改用时：注释掉上面一行、放开下面一行，换成你的公网 IP 或域名）：
# NEXT_PUBLIC_API_BASE_URL=http://101.132.146.92:3001/api/v1

# 说明：NEXT_PUBLIC_* 是「构建时」注入浏览器端的——
#   · next dev 下改完重启 dev server 生效；
#   · 要打包部署，请设好该变量后再 next build。

# 高德地图 Web 端（JS API）Key —— 工作台「真实地图」用。留空则地图回退为路线示意图。
# 对应移动端的 EXPO_PUBLIC_AMAP_JS_KEY / EXPO_PUBLIC_AMAP_SECURITY_CODE。
NEXT_PUBLIC_AMAP_JS_KEY=
NEXT_PUBLIC_AMAP_JS_SECURITY_CODE=
